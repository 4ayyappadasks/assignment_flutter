import 'package:flutter/material.dart';

class ffz_cart extends StatefulWidget {
  const ffz_cart({Key? key}) : super(key: key);

  @override
  State<ffz_cart> createState() => _ffz_cartState();
}

class _ffz_cartState extends State<ffz_cart> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("cart"),);
  }
}
